.next/
node_modules/
coverage/
playwright-report/
test-results/
*.tsbuildinfo
.env.local
.DS_Store
*.log

# Third-party skills installed by the ui-ux-pro-max CLI — 2.8 MB of vendored
# CSVs and scripts, reinstallable with `npx ui-ux-pro-max-cli init --ai claude`.
# Only the skills directory, so project settings stay committable.
.claude/skills/
